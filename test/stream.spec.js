describe('stream', function () {
    var url = 'http://api.wunderground.com/api/8c015f7900afa11d/conditions/q/CA/San_Francisco.json';

    //dtm.stream().query(url);
    //it('should get callback', function () {
    //    var cb = jasmine.createSpy();
    //    dtm.stream().query(url, cb);
    //    waitsFor(function () {
    //        return cb.callCount > 0;
    //    }, 'timed out', 1000);
    //
    //    runs(function () {
    //        expect(cb).toHaveBeenCalled();
    //    });
    //});
});